let minimist,add,subtraction,multiplication,divison,help,version;_b60‍.x([["cli",()=>cli]]);_b60‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_b60‍.w("./add",[["add",["add"],function(v){add=v}]]);_b60‍.w("./subtraction",[["subtraction",["subtraction"],function(v){subtraction=v}]]);_b60‍.w("./multiplication",[["multiplication",["multiplication"],function(v){multiplication=v}]]);_b60‍.w("./divison",[["divison",["divison"],function(v){divison=v}]]);_b60‍.w("./help",[["help",["help"],function(v){help=v}]]);_b60‍.w("./version",[["version",["version"],function(v){version=v}]]);







       async function cli(commandsArr) {
    const argsObj = minimist(commandsArr.slice(2));
    const argsArray = [...argsObj._];

    if (argsObj.version || argsObj.v) {
        return version();
    }

    if (argsObj.help || argsObj.h) {
        return help();
    }

    else if (argsArray.includes('add')) {
        return add(argsObj);
    }
    else if (argsArray.includes('subtraction')) {
        return subtraction(argsObj);
    }
    else if (argsArray.includes('multiplication')) {
        return multiplication(argsObj);
    } else if (argsArray.includes('divison')) {
        return divison(argsObj);
    } else {
        console.error('Is not a valid command');
    }

}